#include <stdio.h>
int main (){
    int a, b, c;
    a = 10;
    b = 20;
    c = a + b; // Calculate the sum of a and b
    printf("A soma de %d e %d é %d\n", a, b, c); // Print the result
    return 0;
} 
